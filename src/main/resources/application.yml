server:
  port: 4545
spring:
  datasource:
    password: root
    url: jdbc:mysql://mysqldb:3306/sys?allowPublicKeyRetrieval=true&useSSL=false
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER


  mail:
    host: smtp.gmail.com
    username: tejamunakala@gmail.com
    password: Talcrds@123
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

userapi:
 messages: 
  regSuccess: Registration Successfull
  invalidCredentials: Invalid Credentials 
  success: Success
  account-inactive: Account is not activated
  
